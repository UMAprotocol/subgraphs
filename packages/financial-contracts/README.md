# Financial Contracts Subgraph

## Deploying the subgraph
- Mainnet: `yarn prepare:mainnet && yarn deploy:mainnet --access-token XXX`
- Kovan: `yarn prepare:mainnet && yarn deploy:mainnet --access-token XXX`