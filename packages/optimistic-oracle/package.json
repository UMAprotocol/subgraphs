{
  "name": "optimistic-oracle-subgraph",
  "version": "0.1.0",
  "description": "Optimistic Oracle Contract Events Subgraph",
  "scripts": {
    "ganache": "ganache-cli -h 0.0.0.0",
    "codegen": "graph codegen",
    "build": "graph build",
    "remove-call-handlers": "node ./scripts/remove-call-handlers.js",
    "prepare:mainnet": "scripts/build-manifest.sh mainnet && yarn codegen && yarn build",
    "prepare:goerli": "scripts/build-manifest.sh goerli && yarn codegen && yarn build",
    "prepare:polygon": "scripts/build-manifest.sh polygon && yarn remove-call-handlers && yarn codegen && yarn build",
    "prepare:optimism": "scripts/build-manifest.sh optimism && yarn remove-call-handlers && yarn codegen && yarn build",
    "prepare:arbitrum": "scripts/build-manifest.sh arbitrum && yarn remove-call-handlers && yarn codegen && yarn build",
    "prepare:boba": "scripts/build-manifest.sh boba && yarn remove-call-handlers && yarn codegen && yarn build",
    "prepare:core-testnet": "scripts/build-manifest.sh core-testnet && yarn remove-call-handlers && yarn codegen && yarn build",
    "prepare:core": "scripts/build-manifest.sh core && yarn remove-call-handlers && yarn codegen && yarn build",
    "prepare:sepolia": "scripts/build-manifest.sh sepolia && yarn codegen && yarn build",
    "prepare:base": "scripts/build-manifest.sh base && yarn remove-call-handlers && yarn codegen && yarn build",
    "prepare:amoy": "scripts/build-manifest.sh amoy && yarn remove-call-handlers && yarn codegen && yarn build",
    "prepare:blast": "scripts/build-manifest.sh blast && yarn remove-call-handlers && yarn codegen && yarn build",
    "deploy:mainnet": "env STAGING=true scripts/deploy.sh mainnet",
    "deploy:goerli": "env STAGING=true scripts/deploy.sh goerli",
    "deploy:polygon": "env STAGING=true scripts/deploy.sh polygon",
    "deploy:optimism": "env STAGING=true scripts/deploy.sh optimism",
    "deploy:arbitrum": "env STAGING=true scripts/deploy.sh arbitrum",
    "deploy:boba": "env STAGING=true scripts/deploy.sh boba",
    "deploy:core-testnet": "env STAGING=true CREATE=true scripts/deploy.sh core-testnet",
    "deploy:core": "env STAGING=true CREATE=true scripts/deploy.sh core",
    "deploy:sepolia": "env STUDIO=true scripts/deploy.sh sepolia",
    "deploy:amoy": "env GOLDSKY=true scripts/deploy.sh amoy",
    "deploy-prod:mainnet": "env STUDIO=true scripts/deploy.sh mainnet",
    "deploy-prod:polygon": "env STUDIO=true scripts/deploy.sh polygon",
    "deploy-prod:boba": "env STUDIO=true scripts/deploy.sh boba",
    "deploy-prod:optimism": "env STUDIO=true scripts/deploy.sh optimism",
    "deploy-prod:arbitrum": "env STUDIO=true scripts/deploy.sh arbitrum",
    "deploy-prod:core": "CREATE=true scripts/deploy.sh core",
    "deploy-prod:base": "env STUDIO=true scripts/deploy.sh base",
    "deploy-prod:blast": "env STUDIO=true scripts/deploy.sh blast",
    "deploy:all-prod": "yarn prepare:mainnet && yarn deploy-prod:mainnet && yarn prepare:polygon && yarn deploy-prod:polygon && yarn prepare:optimism && yarn deploy-prod:optimism && yarn prepare:arbitrum && yarn deploy-prod:arbitrum && yarn prepare:boba && yarn deploy-prod:boba && yarn prepare:core && yarn deploy-prod:core && yarn prepare:base && yarn deploy-prod:base && yarn prepare:blast && yarn deploy-prod:blast"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/UMAprotocol/subgraphs.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/UMAprotocol/subgraphs/issues"
  },
  "homepage": "https://github.com/UMAprotocol/subgraphs#readme",
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.67.3",
    "@graphprotocol/graph-ts": "^0.32.0",
    "mustache": "^4.2.0"
  }
}
